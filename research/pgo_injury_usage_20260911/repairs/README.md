# Admission boundary repair

Preserved exact report01 code and tests before review repairs. A duplicate target identity with one valid count and one invalid count previously left the valid count eligible after value filtering. Count identity multiplicity before value validation. Also require final home_team and away_team to match the saved game instead of relying on game ID and kickoff alone. Report01 remains preserved with its original pins. Report02 replays the same actual source01 bytes with a new execution clock; no new retrieval or retrospective pregame data.
