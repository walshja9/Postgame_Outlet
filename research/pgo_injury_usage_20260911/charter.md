# Locked descriptive injury-usage charter — September 11, 2026

Question: can the existing capture03 pregame unavailable-defender observations be joined by stable identity to subsequently observed 2026 NFL regular-season defensive snap counts?

Grain is game/team/GSIS player ID; population is the saved unavailable_players cohort, not all defenders or all injuries. Pregame roles, reserve context and official statuses are taken unchanged from the hash-verified September 10 capture03. Unknown prior history remains null, including rookies. Team/provider depth summaries describe context; no retrospective expected-role reconstruction. Decision boundary is kickoff minus 60 minutes; both original capture completion and every feature-source capture/publication clock must precede it. The completed opener absent from that capture is explicitly excluded. Repeated captures are not additional samples.

Target is observed defensive snap count, never a predictor. Use the existing nflverse snap-count release source for 2026, preserve actual retrieval clock, HTTP result and raw SHA-256; do not assume that an unavailable release exists. Source publication clock is unknown unless explicitly supplied. A postgame source requires a verified final in the pinned season state, matching game/week/team/opponent, stable GSIS via the preserved roster PFR ID, and capture after that final's observation clock. Display-name-only matches are prohibited. Missing rows remain unknown, never zero. Raw target rows with unresolved identity, duplicate identity, missing counts, invalid counts, wrong event or insufficient timing are excluded explicitly.

Primary metric is admission coverage: joined cohort rows / saved cohort rows, with eligible-final denominator also reported. Secondary counts: missing history, availability UNKNOWN, late/missing pregame evidence, no final, no target row and unresolved/invalid target rows. This is descriptive source admission, with no model, folds, tuning, baseline comparisons or uncertainty estimate. Chronological collection is required; all rows from a game stay together in any future separately declared experiment. Minimum evidence for descriptive linkage is one completely time-safe matched player-game. Zero admitted rows is a valid UNAVAILABLE result and stops interpretation. Any nonzero descriptive result still cannot establish predictive benefit or replacement quality. Historical admission remains BLOCKED FOR FITTING.

Freeze charter before source acquisition and report construction. Preserve previous captures and all failed attempts. Next retrieval is a new output directory. Numerical injury weights, expected unavailable exposure, model changes, causal claims and automatic promotion are out of scope. Required artifacts: executable admission path, raw source/HTTP receipt, row-level exclusions, coverage report, code/charter/input hashes and runnable boundary tests.
