# Future score-error collection — September 12, 2026

This is a collection-only phase, declared before implementation checks or any
production collection. It neither issues outcome ranges nor fits, selects or
promotes a model. Forecast adjustments and numerical ranges remain null;
predictive status is UNAVAILABLE.

One observation is an actual previously durable PGO regular-season forecast,
collected at the actual UTC season-refresh check clock passed to the collector,
strictly before kickoff minus
60 minutes. Preserve the original forecast issue/input clocks, archive pointer,
archive manifest/state pins, producer-code identity and source references.
Capture time is an observation, never an invented provider publication time.
Earlier completed games and first observations at/after T-60 are excluded, even
when an old forecast can be reconstructed. Both perspectives of a game and
repeated unchanged snapshots never create additional calibration games.

Select the latest eligible forecast actually collected before T-60. Append
changed pregame forecasts while retaining earlier observations. Freeze selection
at the cutoff; do not substitute another forecast or roster after outcomes.
A failed selected archive blocks the collection check; no fallback chooses an
easier record. A currently unavailable forecast cannot create an observation.
The actual season-writer durable clock must also precede T-60 for each new
observation or changed selection. An optional failure must retain earlier
evidence and be reported separately from the main forecasts and grading.

Admission verifies unique same-game/team/opponent identity; finite margin,
total and score averages that reconcile; original input <= issue <= original
durable archive <= collection clocks, all strictly before T-60; and exact
archived source bytes and timestamps. Missing clocks or sources are unknown,
never healthy, zero error or manufactured evidence. Keep original availability
and official-starter evidence associated with the selected archive.

Pin the frozen model fit, seed, scoring inputs and relevant executable bytes.
The archive's producer-code hash is an original writer receipt; collection-time
dependency hashes are labeled separately and do not prove past execution.
Recipe cohorts are conservative: changed producer or model/recipe identities
cannot silently pool. Benign producer edits may split a cohort. Any later
compatibility decision needs an explicit new protocol, not a silent merge.

Append a target only after exact replay of a pinned provider response with an
explicit FINAL status, matching the original game identity and event. Keep its
first accepted final-observation clock and source reference immutable. Store
signed prediction-minus-actual errors and absolute errors separately for
home-minus-away margin and combined points, including overtime. An omitted or
invalid result remains unavailable; a later conflicting accepted result blocks
the check. Error summaries are collection diagnostics, not predictive proof.

Count selected unique games separately for each recipe and season. A complete
calibration season requires the complete preserved regular-season schedule and
valid finalized observations for every scheduled game under the same recipe;
merely reaching 500 rows or repeating snapshots is insufficient. The 2026 season
starts partially observed because the two completed openers cannot be admitted.

The September 11 residual recipe and frozen artifacts remain unchanged. Its
context is symmetric 80-percent absolute-residual ranges with two complete
calibration seasons and at least 500 games. A later evaluation requires a
separately frozen protocol, acceptance criteria and a subsequent full season
with at least 150 games across 12 weeks, including 40 in Weeks 1–4. This
collection does not shorten those requirements, issue bounds from historical
diagnostics, establish exchangeability, or authorize acceptance. Any shorter
approach requires a new declaration. No existing forecast, confidence points,
results, sportsbook lines or prior research output may be rewritten.

Verification uses disposable local fixtures only: strict boundary and actual
durability, archive/source tampering, missing clocks, exact finals, immutable
revisions, duplicate identities, complete-season and recipe separation, and
unchanged main forecasts. No live source capture is part of implementation tests.
