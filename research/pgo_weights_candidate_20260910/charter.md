# PGO overlapping inputs and probabilities - September 10, 2026

Locked before this experiment's first fit. The user approved the six-improvement plan, including bounded model-weight and probability comparisons. Status is **EXPERIMENTAL / HOLD**. No main model, saved prediction, penalty weight or confidence allocation is changed by this experiment.

Question one: does removing one overlapping passing-performance block improve the existing regular-season plus postseason-history model's prediction of a regular-season game's final home-minus-away scoring margin, including overtime? One row is one game. Operational prediction time is kickoff minus 60 minutes. Use the frozen postseason run `research/pgo_postseason_candidate/run-20260909-attempt01`, manifest `a58aeff835471182a555e4b926beafd0db01c7c5e3fe19827ddf56bd03f2514a`: its 3,407 original REG feature rows, eight saved folds, and 2,127 held-out predictions. The control is that run's `candidate` column, not its older corrected comparator. Replay the saved control and verify every surviving feature, outcome, identity, order and missing value before fitting. Do not reconstruct historical inputs or fix inherited identity policy during this study.

Exactly three margin arms:

1. `postseason`: unchanged saved control; no refit.
2. `without_qb_passing`: drop exactly `qb_ball_security`, `qb_cpoe`, `qb_current_minus_full`, `qb_epa_per_dropback`, `qb_sack_avoidance`. Keep QB volume, draft, experience and rushing features. This is a passing-performance block ablation, not removal of all QB information.
3. `without_team_passing`: drop exactly `passing_epa_per_play_for` and `sack_avoidance_rate`. Keep defense, rushing, explosive-play, giveaway and other features. These two fields overlap QB passing and sack measures; the broader team offense block is not removed.

Refit each ablation once per expanding 2018-2025 evaluation season and once on all saved training rows (18 new margin fits total). Training rows precede the evaluation season; mirrored perspectives remain together and each original game is scored once. Preserve the existing fold-local preprocessing, Huber delta 1, ridge alpha 200 and four-game history construction. Do not tune hyperparameters or choose additional feature drops after observing results. Require serialized fit replay, exact control replay, fold identity checks and finite predictions. Report all eight folds, pooled MAE, RMSE, bias, winner record, Week 1 and Weeks 1-4. Primary margin metric is paired game-level MAE. A fixed further-study screen requires at least 0.05 points lower MAE than postseason, wins in at least five of eight seasons and a positive lower 95% season-block bootstrap improvement bound (10,000 draws, seed 20260910). Report both candidates regardless of result.

Question two: can a small calibration change improve full home/away/tie probabilities? For each of the three margin arms, compare exactly two curves: (a) existing conditional non-tie logistic mapping with nonnegative slope and zero intercept; (b) conditional non-tie logistic mapping with a fitted intercept and nonnegative slope. Both minimize summed non-tie negative log likelihood plus half the sum of squared fitted parameters (fixed L2 strength 1). Ties are never relabeled as home/away wins. Both use the same training-only tie probability `(training_ties + 1)/(training_games + 2)`, multiplying non-tie probabilities by one minus that mass. Also report a training-only constant three-class reference with add-one smoothing. Intercept fitting is calibration, not a new margin prediction; it can shift the probability favorite away from the margin favorite and this disagreement must be counted. A positive scalar slope alone preserves margin ordering; zero slope flattens it.

Use earlier out-of-fold predictions, not in-sample fitted margins, to fit all calibration parameters. Years 2018-2019 are calibration warmup. Evaluate 2020-2025 on exactly 1,615 common games; for each evaluation season, all calibration training seasons and kickoffs must precede testing. Never train calibration on that test season or full-history predictions. Freeze a final calibration for each curve on all 2,127 out-of-fold rows for prospective use; do not include the 2026 opener. There are 42 fitted calibrators (3 arms x 2 curves x [6 evaluation folds + 1 final]); the constant reference has no fitted optimizer.

Primary probability metric is three-class log loss with probability floor 1e-15; separately report three-class sum Brier (range 0-2), ten fixed selected-team reliability bins with counts, each season, Week 1, Weeks 1-4, ties and favorite-disagreement counts. Report probability-curve comparisons against unchanged postseason with the existing scalar mapping, and intercept versus scalar within each arm, on identical games. A probability further-study screen requires at least 0.005 lower pooled log loss, wins in at least four of six seasons, and a positive lower 95% paired season-block improvement bound (10,000 draws, seed 20260910). These are descriptive fixed screens, not multiplicity-adjusted scientific discoveries. Do not select an arm silently or change model margins based on calibration alone.

All 2018-2025 seasons have already been inspected in related studies: there is no fresh historical holdout. Historical provider revisions, recorded-starter timing and inherited roster/snap identity limitations remain unresolved. Saved feature chronology and fold-local fitting can be verified, but these rows do not recreate historically captured T-60 feeds. Overall historical-vintage verdict remains **REVIEW REQUIRED** even if computations pass. Passing either screen never promotes the model.

Write preparation and fit receipts in new exclusive directories. Pin this charter, relevant code, source manifest/members and every existing immutable issued artifact before and after; mutable season current pointers are excluded explicitly. Preserve failures; construction repairs need a new numbered attempt, and a disappointing result is not permission to rerun/tune. Save exact dropped-feature inventories, all row/fold metadata, predictions, metrics, bootstrap seeds, coefficients, preprocessing, calibrations and portable JSON replay checks. Freeze final fits and calibrations for separately identified prospective comparison only. Any later prospective capture must use verified current inputs, record the matched control and candidate independently, preserve T-60 locks, and leave primary picks unchanged. Runtime integration and publication are owned by the root task.
