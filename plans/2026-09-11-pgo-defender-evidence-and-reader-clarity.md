# PGO defender evidence and reader clarity

User authorization: "proceed with whatever else you recommend." Continue implementation and publication in the existing isolated worktree; the user has delegated routine design choices. Baseline is the September 11 alerts/market release plus verified newer automated season captures. No extra approval gate is needed.

## Design and limits

Prioritize an observed lock-transition gap, missing player-level replacement evidence, and confusing reader labels. Reuse the existing capture, strict identity joins, native links/disclosures and GitHub issue route. Do not fit another variant from two completed games. Do not add a notification service, change notification recipients, rewrite old evidence, promote an experiment or change saved margins, totals, probabilities, confidence points or locked lines.

Named defender inventory is descriptive. Retain the already constructed roster/role/history/availability records in newly dated captures so later snaps can identify who played. A listed backup is not necessarily the actual replacement; observed defensive snaps do not establish assignment, talent or causal injury impact. Unknown history stays unknown. Earlier captures lacking the inventory cannot be reconstructed or relabeled as pregame inventory.

## Tasks

- [ ] Defensive evidence: extend `research/pgo_replacement_depth_20260910/capture.py` additively for new captures with an explicit inventory version and complete player records; preserve existing aggregates and unavailable cohort. Add focused tests in `tests/test_pgo_replacement_depth.py`. Create a small separately versioned descriptive inventory/usage reader under `research/pgo_defender_inventory_20260911/`, reusing the strict identity and final/timing rules of the existing injury usage join. Verify saved state/source bytes; no target admission for missing pregame inventory, late clocks, duplicate identities, missing finals or absent snap rows. Expose pending/missing/zero separately; adjustments stay null. Preserve every prior charter, capture and attempt. Declare scope before any new source observation and retain fresh source/report bytes exclusively if a new descriptive capture is useful. No new fitted model or numerical ranges.
- [ ] Availability boundary: in `pgo_season.py:refresh_availability`, select post-lock context with a fresh clock after the forecast refresh attempt. Preserve any forecast exception and the existing durable lock guard. Tests in `tests/test_pgo_inactive_monitor.py` must cross T-60 during a failed and a successful forecast attempt, verify same-run context collection, and prove frozen pick/confidence preservation. No change to lock times or source authority.
- [ ] Alert confirmation: in `pgo_alerts.py`, share exact ownership validation across list/POST/direct issue reads. Verify a newly returned issue ID directly instead of depending solely on immediate list inclusion, while retaining paginated duplicate checks and rejecting wrong owners/assignees/metadata. If used, retry GET only with a small bound; never repeat POST as an automatic retry. Emit only fixed safe error categories, not raw stderr/provider text. Cover temporary list omission, wrong ownership, ambiguous duplicates and safe error output in `tests/test_pgo_alerts.py`. No extra live commissioning message is needed after issue #8's verified delivery.
- [ ] Reader clarity: in `pgo_season_view.py`, link the next matchup and complete saved slate on off-days using existing anchors and actual counts. Explain winner records at their heading. Distinguish games awaiting finals from genuinely ineligible observations using the existing accuracy reason counts, with unchanged arithmetic and denominators. Keep the old disclosure/anchor IDs and phone layout. Add a short descriptive inventory availability note to the existing defensive research panel if useful; avoid dumping all player rows into the main page. Tests belong in `tests/test_pgo_season_view.py`.
- [ ] Review and release: review agent diffs; run relevant tests once after final source integration, render/inspect desktop and phone, and run the existing full release workflow. Coordinate the source push around active season writers to avoid a preventable publish collision. Verify Pages bytes and the Shopify iframe; retain operational/source evidence and a final handoff. Publish the prior local release record with this source batch. Do not claim new predictive accuracy from software or descriptive linkage checks.

## Ownership and verification

Market agent owns defender capture/new research reader and tests. Delivery agent owns the T-60 boundary/alerts and corresponding tests/docs. Root owns view/integration/workflows and this plan. Independent reviewer checks final changes. No agent commits, pushes, edits shared workflows or sends notifications.

Use the existing unittest framework and meaningful red-to-green regressions. Local Python 3.14 has the installed model dependencies; CI uses Python 3.12 with `requirements-pgo.txt`. Preserve byte-sensitive research paths with explicit Git attributes and verify staged blobs. The full release gate remains `.github/workflows/update-board.yml`, including its supplemental research suites. Future schedule runs remain ongoing maintenance, not a reason to wait indefinitely after a verified release.

Considered and deferred: retaining failed unpublished captures as a separate workflow artifact, a new score-range calibration series, another fitted weight/penalty/opponent variant, filters/frameworks and an external watchdog. These are not necessary for this bounded release; existing evidence does not justify model promotion.
