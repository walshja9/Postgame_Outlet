# PGO off-day source refresh and readiness

Resume baseline: `7694cd0` (prior local release record plus canonical `832444e`). User authorized continuing recommended implementation and publication. Routine design choices are delegated; no additional approval gate is needed.

## Evidence and choice

At the verified September 12 04:23:34 UTC state, primary grading was READY but replacement evidence was BLOCKED: roster/depth downloads were 27.51 hours old. The next game was more than 24 hours away, so the normal forecast-availability path correctly did not fetch them. The pure defender capture correctly rejected stale input. Fix the missing source-maintenance path, not the capture validity rule or QB forecast window.

The alert assessor also treated blocked optional monitors and a failed rollover verifier as healthy. The UI's unqualified Automation: READY concealed that distinction. Keep main grading independent while making the actual component failures visible.

## Implementation

- [x] Source maintenance (market agent): `pgo_season.py` and focused `tests/test_pgo_replacement_refresh.py`. Add a small source-only helper before the final checked_at/locked-record restoration/source normalization in refresh. If future unlocked games exist, reuse current/retained roster and depth references and fetch only missing or older-than-24-hour inputs through existing fetch_source. Retain successful receipts on partial failure. Keep source errors confined to descriptive replacement evidence; preserve the previous payload on failure. An explicit source-check status may connect maintenance failure to the existing independent experiment wrapper. Keep capture.capture deterministic for archived-source replay. No select_roster/build_next call, QB reranking or widening of the forecast update window. Fresh identical bytes retain their hash with a real new download time; never replace provider observation time with download time. Regression cases: stale off-day inputs; no duplicate fetch while fresh; partial failure with primary grades/picks intact; no eligible future games; failed/late source evidence; existing T-60 protection.
- [x] Alerts (delivery agent): `pgo_alerts.py`, `tests/test_pgo_alerts.py`, relevant `docs/pgo-alerts.md`. Add fixed safe nonurgent conditions for explicitly BLOCKED optional penalty/totals/weights/defender updates. Do not alert on experimental HOLD or historical fitting admission. Treat the rollover observation outcome as a required pipeline stage for notification/resolution. Preserve owner, deduplication, no-repeat-POST and safe diagnostics. Test failed monitors, healthy descriptive/HOLD, failed rollover, successful recovery and no spurious alert for absent optional components. Root wires the workflow stage ID and outcome argument/environment alongside the existing stages.
- [x] Reader clarity (root): `pgo_season_view.py`, `tests/test_pgo_season_view.py`. Scope top health text to main picks/grades, and add one linked sentence for a failed independent monitor using its existing disclosure (no new dashboard). Give the defender disclosure an anchor. In the existing off-day next-game line, show prediction lock and the 24-hour availability-check window; within that window show the existing saved check status. Use state observation time, existing formatting/anchors and escaped values. Never claim a scheduled check already happened merely because its window opened. Check before/open/locked and no-next-game states without mutating inputs.
- [ ] Integration/release (root): add the new test module to the scheduled gate; update workflow contract tests and bytes attributes. Review all diffs, run the exact scheduled gate and full release workflow. Render and inspect phone/default page, perform one normal production refresh, reproduce the newly saved defender inventory from archived sources, verify main/locked picks and grades, and compare public bytes with Git. Publish the prior local release record with this batch, coordinating source pushes around active writers. Preserve failure receipts and prior captures, forecasts, quotes, charters and attempts.

## Bounds and verification

Reuse existing Python/unittest, source archives, native disclosures and GitHub alerts. No dependencies, external notification destination, model fitting/promotion, fabricated injury effects, historical backfill or changed confidence allocations. Keep the final publication guard and chronology checks intact. Broader failed-publication artifact packaging, a new model variant and an external watchdog remain deferred; they are not necessary to fix the observed source/health gaps.

Use actual red-to-green regressions for changed logic, then the exact unittest command in `.github/workflows/update-season.yml`. Full gate: `.github/workflows/update-board.yml`, main discovery plus its two existing supplemental suites. Independent review and live publication are separate claims. A subsequent scheduled maintenance run is not unfinished release work.

## Local integration evidence

The exact scheduled gate passed all 230 tests on Python 3.14. The six new production-path source tests cover off-day refresh, receipt reuse, partial-failure recovery, conflicting receipt hashes, future/late capture clocks, and preservation at the lock boundary. The older rollover fixture now explicitly isolates source maintenance. The relevant 50-test season group also passed with zero unmocked network requests. No dependency or model formula changed.

Desktop and 390px-phone preview checks passed: the PGO panel has no width overflow, failed defender information opens its existing disclosure, and the next-game line shows the saved lock/check window. Local evidence is under output/source-refresh-20260912/. Live source recovery, full Python 3.12 CI, and deployed-byte verification remain release gates.

Independent review approved both the presentation/alerts/workflow diff and the final source-maintenance diff, including two-tick recovery and late-lock preservation. No actionable findings.
