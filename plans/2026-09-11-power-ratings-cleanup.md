# Power Ratings cleanup

The user requested another cleanup of the live Power Ratings page. This continues the approved McCabe slate/cobalt theme and publication scope. Use the existing native-theme and app worktrees; preserve all model inputs, issued values, archived evidence and iframe security/resize behavior.

The live phone layout puts the iframe 1,596 pixels below the page top at 390 pixels wide. The native wrapper repeats the app title and method explanation; the PGO panel adds another long introduction, eight equally prominent navigation links and expanded availability lists. A compact introduction and existing native disclosures address these problems without adding a navigation framework or dependency.

- [x] Native wrapper: in `Postgame_Outlet-sitewide-mccabe-20260909`, update `sections/postgame-ratings.liquid`, `assets/postgame-content.css` and the page template only as needed. Keep one compact native heading and orientation sentence, a visible experimental/non-QB limitation, short links and an expandable explanation. Pull and hash the current live files before editing; retain the iframe and its JavaScript bridge. Update relevant existing theme assertions and run Theme Check against the unchanged baseline.
- [x] Embedded PGO panel: simplify `pgo_season_view.py` and `docs/pgo-theme.css`. Keep Game day, Picks, Rankings and Records prominent; group secondary destinations in a native disclosure. Condense normal freshness metadata and repeated definitions; show active warnings and exact clocks. Collapse long availability lists/sources without losing any information. Check the existing season-view tests and preservation contracts.
- [x] Verify desktop and phone layouts, keyboard disclosures, tab/jump navigation and no viewport overflow in preview. Confirm unchanged numbers and archives. Review the combined diff, publish the reviewed app and allowlisted native files, verify CI/Pages/live readback, and record results here. Screenshot capture is currently failing in the in-app browser; retain measured DOM evidence and retry in the preview before making visual claims.

No model refit, numerical injury adjustment, new data source, dependency, full theme replacement or archive redesign is part of this pass.

Local validation: 144 affected app tests passed in 242.575 seconds; 14 native theme tests passed. Theme Check remained at 428 existing findings with zero introduced findings. Independent reviews approved both diffs. Preview navigation targets and keyboard disclosures passed at 1280 and 390 pixels; no document overflow. The native phone iframe moved from 1596 to 535 pixels from the top in the initial preview measurement. Browser screenshot capture remained unavailable on live, native preview and app preview, so these are DOM/interaction checks rather than screenshot approval.


Published September 11, 2026. App source `c2b52e6` was integrated as `05321b2fd420cda080a1f0e3450539943d0fff42`. [Full CI and guarded publication](https://github.com/walshja9/Postgame_Outlet/actions/runs/34560526211) succeeded: 854 tests passed, one skipped. The generated app commit `ff6c19c15cdd9092daa39d44620a14d6838300f9` [deployed successfully](https://github.com/walshja9/Postgame_Outlet/actions/runs/34561622362); both public HTML files and CSS exactly matched its Git blobs. Independent review confirmed preserved tables, identifiers, saved values and source clocks.

The native theme commit is `f0de9e948786d895d3b89905a241545d7ed68c0c` in the existing native worktree. Exactly three allowlisted theme files were uploaded to live theme `159107678440` and matched the live readback; the origin-checked iframe bridge stayed byte-identical. The current page-note date is September 11, and the iframe cache marker is `05321b2`.

Final live browser checks confirmed all eight navigation targets, keyboard expansion and the new controls within Shopify. At a 390-pixel browser width, the host document measured 375 pixels and the embedded document 320 pixels, matching its iframe; game cards fit inside it. The native board start measured 535 pixels from the page top versus 1596 before cleanup. Full details and source links remain accessible. These checks used DOM geometry, visible text and keyboard interactions because screenshot capture was unavailable.

Receipts and preview artifacts are in each worktree's ignored `output/ratings-cleanup-20260911/`; the app folder contains the combined receipt and full CI test log. Both temporary preview servers were stopped. No model data, fitting or issued forecast was changed by this cleanup.
